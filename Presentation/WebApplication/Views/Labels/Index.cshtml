@model WebApplication.Models.Labels.LabelsViewModel;

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

@if (Model.AvailableGroupsSelectList.Any())
{
    <p>
        <a asp-action="Create">Create New</a>
    </p>

    <h4> Filter </h4>
    <form asp-action="Index" method="get">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="form-group">
            <label asp-for="Group" class="control-label"></label>
            <select asp-for="Group" name="Month" value="Model.Month" class="form-control" asp-items="@Model.AvailableGroupsSelectList"></select>
        </div>
        @*<div class="form-group">
            <label asp-for="Month" class="control-label"></label>
            <select asp-for="Month" name="Month" value="Model.Month" class="form-control" asp-items="ViewBag.Months"></select>
        </div>
        <div class="form-group">
            <label asp-for="Year" class="control-label"></label>
            <select asp-for="Year" name="Year" value="Model.Year" class="form-control" asp-items="ViewBag.Years"></select>
        </div>*@

        <div class="form-group">
            <input type="submit" value="Filter" class="btn btn-default" />
        </div>
    </form>

    <table class="table">
        <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LabelLabel.Name)
            </th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        @foreach (var item in Model.Labels)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
}
else
{
    <h4> Need to create a group first before create new Label</h4> <a asp-controller="Groups" asp-action="Create">Create Group</a>
}
